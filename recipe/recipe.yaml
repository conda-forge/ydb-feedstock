schema_version: 1

context:
  name: ydb
  version: "3.26.5"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/ydb-${{ version }}.tar.gz
  sha256: 847a47f8fae412f4873d64d9e21f8e529a8c15a06992e2522a273fc5efd5890d

build:
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - grpcio >=1.42.0
    - packaging
    - protobuf >=3.13.0,<7.0.0
    - aiohttp <4
tests:
  - python:
      imports:
        - ydb
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: http://github.com/ydb-platform/ydb-python-sdk
  summary: YDB Python SDK
  license: Apache-2.0
  license_file: LICENSE
  repository: http://github.com/ydb-platform/ydb-python-sdk
  documentation: https://ydb-platform.github.io/ydb-python-sdk/

extra:
  recipe-maintainers:
    - xylar
