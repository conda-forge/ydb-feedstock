schema_version: 1

context:
  name: ydb
  version: "3.28.4"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/ydb-${{ version }}.tar.gz
  sha256: 272d620d67704642d1fa44564597ccd327ad9d1c5d6a0a099b4abb8662795333

build:
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - grpcio >=1.42.0
    - packaging
    - protobuf >=3.13.0,<7.0.0
    - aiohttp <4
tests:
  - python:
      imports:
        - ydb
      pip_check: true
      python_version: ${{ python_min }}.*

about:
  homepage: http://github.com/ydb-platform/ydb-python-sdk
  summary: YDB Python SDK
  license: Apache-2.0
  license_file: LICENSE
  repository: http://github.com/ydb-platform/ydb-python-sdk
  documentation: https://ydb-platform.github.io/ydb-python-sdk/

extra:
  recipe-maintainers:
    - xylar
